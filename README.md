# Project

## Overview
One paragraph describing what this project is.

## Local development
1. Run `./scripts/bootstrap.sh`
2. Run the dev server command for this stack
3. Open the local URL

## Checks
Run `./scripts/check.sh`

## Deployment
Describe how it is deployed (for example Vercel) and where env vars are set.

## Project status
Human readable explenation of the project status, clearly stating what is done and what is not done

